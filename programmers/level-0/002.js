// 두 수의 나눗셈
function solution(num1, num2) {
  const answer = parseInt((num1 / num2) * 1000);
  return answer;
}

// 숫자 비교하기
function solution(num1, num2) {
    let answer;
    return answer = (num1 == num2) ? 1 : -1;
}
